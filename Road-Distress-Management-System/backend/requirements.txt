annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
click==8.4.1
colorama==0.4.6
fastapi==0.138.0
greenlet==3.5.2
h11==0.16.0
idna==3.18
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn[standard]==0.49.0
openpyxl==3.1.5
reportlab==5.0.0
alembic==1.18.4
python-multipart==0.0.32
opencv-python==4.13.0.92
imageio==2.37.4
imageio-ffmpeg==0.6.0
numpy==2.5.0
torch==2.12.1
torchvision==0.27.1

# --- YOLOX runtime (for the trained pavement/signage checkpoints, see
# backend/models/webcam_test_combined.py and app/services/live/ -- the live USB
# camera feature) ---
# yolox's own PyPI metadata additionally declares onnx==1.8.1 (pinned, no wheel
# for this Python version), onnxruntime==1.8.0, onnx-simplifier==0.3.5,
# tensorboard, and scikit-image as dependencies. Those are only used by yolox's
# training/ONNX-export/COCO-eval tooling, not by the inference path this
# project needs (get_exp / ValTransform / postprocess), and onnx==1.8.1 in
# particular cannot be resolved on current Python. Install yolox with --no-deps
# to skip that unusable dependency set:
#   pip install cmake ninja                     (build tools yolox's setup.py needs)
#   pip install yolox==0.3.0 --no-deps --no-build-isolation
# pycocotools is still required at import time by yolox.data (even for
# non-COCO use) and DOES install cleanly, so it's listed normally below.
pycocotools>=2.0.2
loguru==0.7.3
tabulate==0.10.0
thop==0.1.1.post2209072238
tqdm==4.68.4
